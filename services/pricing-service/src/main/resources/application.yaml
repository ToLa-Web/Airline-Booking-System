server:
  port: 5006

spring:
  application:
    name: pricing-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_pricing_db
    username: root
    password: pass1234
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true