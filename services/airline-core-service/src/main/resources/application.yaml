server:
  port: 5002

spring:
  application:
    name: airline-core-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_db
    username: root
    password: pass1234
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true